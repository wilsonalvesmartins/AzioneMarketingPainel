version: '3.8'
services:
  azione-app:
    build: .
    container_name: azione-marketing-app
    init: true
    restart: unless-stopped
    stop_grace_period: 30s
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    volumes:
      - ./data:/app/data
